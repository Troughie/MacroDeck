# Assets

Place the following files here:
- `icon.png` — 256x256 app icon (PNG)
- `icon.ico` — Windows ICO format for installer
- `tray-icon.png` — 16x16 or 32x32 tray icon (PNG)

These are referenced in `electron/main.ts` and `package.json` build config.
